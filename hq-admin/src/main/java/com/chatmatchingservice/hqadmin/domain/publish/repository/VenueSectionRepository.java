package com.chatmatchingservice.hqadmin.domain.publish.repository;

import com.chatmatchingservice.hqadmin.domain.publish.entity.VenueSection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VenueSectionRepository extends JpaRepository<VenueSection, Long> {
}